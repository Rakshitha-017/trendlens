# TrendLens — Python Dependencies
# Install: pip install -r requirements.txt && pip install hdbscan
# Python 3.11+ recommended (not 3.13 — PyTorch not yet supported)
# Tested on: Python 3.12, Linux x86_64

# ── Deep Learning Framework ────────────────────────────────────────────────
torch==2.13.0
torchvision==0.28.0

# ── Vision-Language Models ─────────────────────────────────────────────────
transformers==5.14.1          # CLIP, BERT, BLIP, BLIP-2
safetensors==0.8.0
tokenizers==0.22.2
huggingface_hub==1.26.0
Pillow==12.3.0

# ── Text Embeddings (RAG encoder) ──────────────────────────────────────────
sentence-transformers==5.6.1  # all-MiniLM-L6-v2 for FAISS retrieval

# ── Vector Search (RAG index) ──────────────────────────────────────────────
faiss-cpu==1.14.3             # IndexFlatIP dense retrieval; swap to faiss-gpu on CUDA

# ── LLM Reasoning (RAG synthesis) ─────────────────────────────────────────
google-genai==2.16.0          # Gemini gemini-2.0-flash
python-dotenv==1.2.2          # .env loader for GOOGLE_API_KEY

# ── Gradient Boosting (Popularity Model) ──────────────────────────────────
lightgbm==4.7.0               # Post-level log1p(likes+comments) regressor

# ── Model Quantisation (BLIP-2 on GPU) ────────────────────────────────────
bitsandbytes==0.50.0          # 8-bit loading of blip2-opt-2.7b on GPU (optional on CPU)

# ── Clustering & Dimensionality Reduction ─────────────────────────────────
# hdbscan: install separately due to build dependencies
# pip install hdbscan==0.8.44
umap-learn==0.5.12
pynndescent==0.6.0            # Required by umap-learn
numba==0.66.0                 # Required by both umap-learn and hdbscan
llvmlite==0.48.0

# ── Scientific Computing ───────────────────────────────────────────────────
numpy==2.4.6
scipy==1.18.0
scikit-learn==1.9.0
pandas==3.0.5

# ── Visualisation ──────────────────────────────────────────────────────────
matplotlib==3.11.1

# ── Utilities ──────────────────────────────────────────────────────────────
tqdm==4.70.0
PyYAML==6.0.3
requests==2.34.2
